<nav class="navbar navbar-default navbar-head" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">WHY?</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              <%= image_tag(current_user.profile.image.url, class: 'user-icon') %><%= current_user.username %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><a href="<%= edit_profile_path(current_user.profile) %>">Account</a></li>
              <li><a href="<%= destroy_user_session_path %>" data: { method='delete' } >Logout</a></li>
            </ul>
          </li>
        <% else %>
          <li><a href="<%= new_user_session_path %>">Login</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>